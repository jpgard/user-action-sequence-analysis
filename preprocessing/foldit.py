"""Functions to preprocess foldit data into user-puzzle sequences"""

import pandas as pd

def get_pid_uid_sequences(infile, max_len=1000):
    """
    Fetch a hierarchical dictionary with keys [pid][uid] and values which are sequences of ac
    :param infile:
    :return:
    """
    #todo
    return